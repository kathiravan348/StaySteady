// The earliest date reports can start: the first day of mock price history (M-04). The server checks
// the same limit; this lets the screen say so before asking.
export const PRICE_HISTORY_START = '2022-01-03';
